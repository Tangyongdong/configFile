spring:
  profiles: test

#datasource
datasource.ip_port: localhost:3306
datasource.username: root
datasource.password: root1234

aop.auto: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://test-eureka:8761/eureka/
  instance:
      prefer-ip-address: true
    client:
      register-with-eureka: true
      fetch-registry: true
spring:
  profiles: test

eureka:
  client:
    serviceUrl:
      defaultZone: http://test-eureka:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true